{
  "name": "ui-react",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "bun run --cwd packages/dev format && bun run --cwd packages/ui format && bun run --cwd packages/theme format",
    "lint": "bun run --cwd packages/dev lint && bun run --cwd packages/ui lint && bun run --cwd packages/theme lint",
    "dev": "bun run --cwd packages/dev dev",
    "build": "bun run --cwd packages/dev build && bun run --cwd packages/ui build && bun run --cwd packages/theme build"
  },
  "devDependencies": {
    "@types/node": "^20.10.3",
    "@types/web": "^0.0.125",
    "@typescript-eslint/eslint-plugin": "^6.13.2",
    "@typescript-eslint/parser": "^6.13.2",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2",
    "tsup": "^8.0.1"
  }
}
